apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: indicators-api-app
  name: indicators-api-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: indicators-api-app
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: indicators-api-app
    spec:
      containers:
      - image: nodeapitest.azurecr.io/aksnode/indicators-api-image:v1
        name: webapp
        imagePullPolicy: Always
        resources: {}
        ports:
          - containerPort: 3000 
status: {}

---

apiVersion: v1
kind: Service
metadata:
  name: indicators-api-app
  labels:
    run: indicators-api-app
spec:
  ports:
  - port: 3000
    protocol: TCP
  selector:
    app: indicators-api-app
  type: LoadBalancer